<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- LINK BOOTSTRAP -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <!-- LINK css -->
   <link rel="stylesheet" href="/assets/style.css">

   
  <title>App - Listas</title>
</head>
<body class="bg-dark text-white" data-bs-theme="dark">
  <div class="container d-flex flex-column justify-content-between h-100">

    <div class="header"><%- include('partials/header') %></div>

    <div class="main h-100">
      <h1 class="mb-4">Listas de Tarefas</h1>
      <hr>
      <% if (allTaskLists.length === 0) { %>
       <p>Não há nada aqui por enquanto... que tal criar sua primeira lista ?</p>
      <% } %>
        <div class="mt-4">
          <% allTaskLists.forEach(taskList => { %>
            <a href="/app/<%= taskList.id %>" class="btn d-flex algin-items-center justify-content-between border border-secondary rounded p-2 mb-3 task-list">
              <span class="text-body-secondary"><%= taskList.name %></span>
              <form action="/app/delete/<%= taskList.id %>" method="POST">
                <button class="btn btn-outline-danger btn-sm">Excluir</button>
              </form>
            </a>
          <% }) %>
        </div>
      <a class="btn btn-outline-info" href="/app/new-list">Criar uma nova lista</a>
      
    </div>

    <div class="footer"><%- include('partials/footer') %></div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>